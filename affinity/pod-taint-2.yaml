apiVersion: v1
kind: Pod
metadata:
  name: taint-2
  namespace: prod 
  labels:
    app: taint
spec:
  containers:
  - name: pod-taint
    image: "busybox"
    command: ["sleep", "3600"]
  tolerations:
  - key: "key"
    operator: "Exists"
    effect: "NoSchedule" 

