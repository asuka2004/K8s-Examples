apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod-redis
  namespace: test
spec:
  selector:
    matchLabels:
      app: store
  replicas: 5
  template:
    metadata:
      labels:
        app: store
    spec:
      containers:
      - name: redis-server
        image: redis:3.2-alpine
      affinity:
        podAntiAffinity: 
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector: 
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                    - store
              topologyKey:  "zone"        
