apiVersion: v1
kind: Pod
metadata:
  name: pod-mount-secret-1
  namespace: test 
  labels:
    app: pod-mount-secret
spec:
  volumes:
  - name: pod-vol
    secret: 
      defaultMode: 0400
      secretName: "db-user"
      items:
      - key: password
        path: pwd.md  
  containers:
  - name: pod-moount-secret-1
    image: "busybox"
    command: ["/bin/sh","-c","sleep 3600"]
    resources:
      limits:
        cpu: 10m
      requests:  
        cpu: 5m 
    volumeMounts:
    - name: pod-vol
      mountPath: /app

